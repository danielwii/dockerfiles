version: '3'

services:
  phpmyadmin:
    image: "phpmyadmin/phpmyadmin:4.8"
    ports:
      - "20080:80"
    environment:
      - "PMA_HOST=host.docker.internal"
  mysql:
    image: "mysql:5.7"
    ports:
      - "3306:3306"
    environment:
      - "MYSQL_ROOT_PASSWORD=password"
    volumes:
      - "$HOME/Development/data/mysql:/var/lib/mysql"
